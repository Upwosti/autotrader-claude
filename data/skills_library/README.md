# Skills Library

Stores learned behavior patterns per pair and regime.
Format: JSON files per pair + global skills index.

Files:
- skills_index.json — all pairs, confidence scores, best behaviors
- {PAIR}_skills.json — per-pair behavior history
